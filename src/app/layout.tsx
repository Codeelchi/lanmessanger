import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "LAN Chat - Lightweight Team Messaging",
  description: "Fast, lightweight messaging for your team. Chat privately or in groups, broadcast announcements, and stay connected on your local network.",
  keywords: ["LAN Chat", "messaging", "team chat", "private messaging", "group chat"],
  authors: [{ name: "LAN Chat Team" }],
  icons: {
    icon: "https://z-cdn.chatglm.cn/z-ai/static/logo.svg",
  },
  openGraph: {
    title: "LAN Chat - Lightweight Team Messaging",
    description: "Fast, lightweight messaging for your team and private conversations.",
    type: "website",
  },
  manifest: "/manifest.json",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-background text-foreground overflow-hidden`}
      >
        {children}
      </body>
    </html>
  );
}
